# データベース設定
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/atelier_resource_mgmt?schema=public"

# サーバー設定
PORT=3000
NODE_ENV=development

# CORS設定
CORS_ORIGIN=http://localhost:5173

# 🔵 Repository設定（Phase 1-B: TASK-0015A）
# prisma: Prisma実装を使用（本番環境用、PostgreSQL接続）
# memory: In-Memory実装を使用（テスト用、データベース不要）
REPOSITORY_TYPE=prisma
